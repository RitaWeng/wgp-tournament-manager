{
    "name": "wgp-score-relay",
    "version": "0.0.1",
    "private": true,
    "description": "WGP 線上成績回報後端（成績中繼站）— 規格見 docs/online-score-reporting-plan.md",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:local": "wrangler d1 execute wgp_score_relay --local --file=./schema.sql",
        "test": "node test/api.test.mjs",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "hono": "^4.6.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20260101.0",
        "playwright": "^1.61.1",
        "typescript": "^5.1.6",
        "wrangler": "^4.0.0"
    }
}
